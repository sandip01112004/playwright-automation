services:
  automation:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - HEADLESS=true
    volumes:
      - ./playwright-report:/app/playwright-report
      - ./test-results:/app/test-results
